{
  "name": "claude-recall",
  "version": "6.0.6",
  "description": "Session awareness statusline for Claude Code",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  },
  "engines": { "node": ">=20.0.0" }
}
