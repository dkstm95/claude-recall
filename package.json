{
  "name": "claude-recall",
  "version": "6.4.3",
  "description": "Session awareness statusline for Claude Code",
  "type": "module",
  "scripts": {
    "build": "tsc && npm run sync:plugin",
    "sync:plugin": "node scripts/sync-plugin.mjs",
    "check:dist": "node scripts/check-dist.mjs",
    "test": "npm run check:dist && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  },
  "engines": { "node": ">=20.0.0" }
}
